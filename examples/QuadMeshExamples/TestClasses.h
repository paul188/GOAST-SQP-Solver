
template<typename ConfiguratorType>
class TestClass : public BaseOp<ConfiguratorType::VectorType,ConfiguratorType::VectorType>{

    protected:
        
    public:
        TestClass()
        void apply(const VectorType &in, VectorType &Dest) const{

        }

};